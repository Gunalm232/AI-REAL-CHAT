# Server
PORT=3000

# OpenAI (required for AI Mode)
# Replace with your real key: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Safe default; change if you have access to newer models
OPENAI_MODEL=gpt-3.5-turbo

# MySQL (only if using server.js)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=chat_app
